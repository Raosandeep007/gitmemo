// Auth service types are intentionally removed in GitHub-backed mode.
// This stub keeps old import paths compilable during migration.

export type SignInRequest = never;
export type SignInResponse = never;

export const SignInRequestSchema = {};
export const SignInResponseSchema = {};
